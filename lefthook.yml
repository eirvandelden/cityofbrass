pre-commit:
  commands:
    rubocop:
      tags: rubocop
      glob: "*.rb"
      run: bundle exec rubocop --force-exclusion {staged_files}

pre-push:
  commands:
    pre_push_checks:
      run: bin/pre_push_checks
