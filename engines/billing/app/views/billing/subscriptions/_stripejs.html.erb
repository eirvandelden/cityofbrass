<% content_for :footer do %>
  <%= javascript_include_tag "https://js.stripe.com/v2/" if user_signed_in? %>
  <%= tag :meta, :name => "stripe-key", :content => ENV['STRIPE_PUBLISHABLE_KEY'] if user_signed_in? %>
<% end %>
