<div id="form_div">
<%= form_with model: [@parent_object, @section], url: nested_resource_form_path(@parent_object, @section, :sections), local: false, html: {'data-no-transition-cache' => ''}, builder: ApplicationFormBuilder do |f| %>
  <fieldset>
    <div class="row inline text-right">
      <div class="small-12 columns">
        <label class="inline-label-flat"><%= f.object.section_type.humanize %></label>
      </div>
    </div>
    <%= f.hidden_field :sort_order %>
    <%= f.hidden_field :section_type %>
    <div class="row">
      <div class="small-12 columns">
        <%= render :partial => "storybuilder/sections/form/#{f.object.section_type}", :locals => { f: f } %>
      </div>
    </div>
  </fieldset>
  <%= render "layouts/form/save_and_cancel" %>
<% end %>
</div>
