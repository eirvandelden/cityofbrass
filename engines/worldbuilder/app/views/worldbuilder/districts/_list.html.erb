<% @districts.each do |district| %>
<%= render 'district', district: district %>
<% end %>

<div class="row">
  <div class="small-12 columns text-center">
    <%= paginate @districts, :window => 2 %>
  </div>
</div>
