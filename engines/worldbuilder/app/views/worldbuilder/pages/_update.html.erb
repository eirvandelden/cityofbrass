<%= simple_form_for @page, url: worldbuilder.district_page_path(@district.slug, @page), :remote => true, :html => {'data-no-transition-cache' => ''} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
