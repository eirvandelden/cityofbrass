<div class="row">
  <div class="medium-10 medium-centered columns">
    <main>
      <h1><%= t("importer.imports.index.title") %></h1>
      <p><%= t("importer.imports.index.description") %></p>
      <%= link_to t("importer.imports.index.new_preview"), new_preview_path, class: "button" %>

      <%= render "importer/imports/history", imports: @imports, admin: false %>
    </main>
  </div>
</div>
