<% provide(:title, 'FAQs') %>

<% content_for :breadcrumb do %>
  <%=  breadcrumb [
          { "FAQs" => faqs_path },
          { "New FAQ" => nil }
      ] %>
<% end %>

<div class="row">
  <div class="medium-10 medium-centered columns" id="brass_form">
    <%= form_with model: @faq, builder: ApplicationFormBuilder do |f| %>
      <%= render :partial =>'form', :locals => { :f => f } %>
    <% end %>
  </div>
</div>
