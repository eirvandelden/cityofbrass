if($('#character_<%= @character.id %>').length > 0){
  $('#character_<%= @character.id %>').remove();
}

if($('#badge_<%= @character.id %>').length > 0){
  $('#badge_<%= @character.id %>').remove();
}
