<% provide(:title, "#{@parent_object.name}") %>
<% type_array = type_split(@parent_type) %>
<% content_for :breadcrumb do %>
  <%=  breadcrumb [
    { type_array[0] => nil },
    { type_array[1] => route_collection_path(@parent_type.pluralize) },
    { @parent_object.name => nil },
    { @movement.name => nil }
  ] %>
<% end %>

<div class="row">
  <div class="medium-2 columns">
    <%= render :partial => "entitybuilder/menus/manage",
      :locals => {
        :view_link => route_path(@parent_object),
        :about_link => edit_route_path(@parent_object),
        :base_link => route_path(@parent_object),
        :active_link => 'movements'
      } %>
  </div>

  <div class="medium-10 columns">
    <h2><%= @movement.name %></h2>
    <hr class="faded">
    <p><i class='fa fa-angle-double-left'></i> <%= link_to "Return to Index".html_safe, :back %></p>
  </div>
</div>
