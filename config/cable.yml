development:
  adapter: async

test:
  adapter: async

# The app has no channels. Adding one means choosing an adapter that works
# across processes first — async only broadcasts within a single process.
production:
  adapter: async
